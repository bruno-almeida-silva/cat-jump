JAVA 11 in Ubuntu

Install [ open terminal ]

$ sudo apt-get update
$ sudo apt-get install build-essential
$ sudo apt-get install openjdk-11-jdk
$ java -version

Define JAVA_HOME
$ sudo update-alternatives --config java # gets installation path

/usr/lib/jvm/java-11-openjdk-amd64 # copy path before /bin...

$ nano ~/.bashrc

JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
export JAVA_HOME
export PATH=$PATH:$JAVA_HOME

$ source ~/.bashrc
$ echo $JAVA_HOME

Maven
$ sudo apt-get install maven
$ mvn --v